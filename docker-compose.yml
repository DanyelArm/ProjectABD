services:
  mongo:
    image: mongo:7.0
    container_name: mongo_zips
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
